


{{#each items}}
<div class="l-1-3">
  <a href="/{{category}}/{{_id}}"><h2>{{title}}</h2></a>
  <a href="/authors/{{author.authorId}}"><h3>{{author.firstName}} {{author.lastName}}</h3></a>
  <h3>{{date}}</h3>
  <a href="{{category}}"><h3>{{category}}</h3></a>
  <!-- {{#each body }} -->
      <!-- <p> {{p}} </p> -->
  <!-- {{/each}} -->
  <p>{{ body.0.p }}</p>
</div>
{{/each}}
