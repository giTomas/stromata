

<div class="l-1-3">
{{#each items}}
  <h2>{{title}}</h2>
  <h3>{{date}}</h3>
  <h3>{{category}}</h3>
  {{#each body }}
      <p> {{p}} </p>
  {{/each}}
  <p> {{ body.p }} </p>
  <a href="/articles/{{_id}}">articles</a>
{{/each}}
</div>
